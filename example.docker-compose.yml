services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
    networks:
      - tuneguess

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - tuneguess

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      FRONTEND_URL: http://localhost:8080
      CORS_ORIGIN: http://localhost:8080
      REDIRECT_URI: http://localhost:8080
      PORT: "5000"
      ADMIN_PASSWORD: "admin-local-secret"
    ports:
      - "5000:5000"
    networks:
      - tuneguess

volumes:
  caddy_data:
  caddy_config:

networks:
  tuneguess:
    driver: bridge
